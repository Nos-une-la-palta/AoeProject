<div class="container edit">
<h2>Edit <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <div class="row">
    <div class="col">
        <div class="field">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", type:"imput", placeholder:"Email" %>
        </div>
        <div class="field">
          <% if @minimum_password_length %>
          <%= f.password_field :password, autocomplete: "new-password", placeholder:"Nueva Contraseña" %>
          <em>La contraseña debe tener al menos <%= @minimum_password_length %> caracteres</em>
        <% end %>
        </div>

      </div>
        <div class="col">
          <div class="field">
            <%= f.text_field :nick,  class:"form-inline", placeholder:"Nick", type:"imput" %>
          </div>
          <div class="field">
            <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder:"Confirmar nueva Contraseña" %>
          </div>
       </div>
    </div>
    <hr class="hr">
    <div class="row">
      <div class="col-3">
        <div class="field">
          <%= f.label :image, "Añadir avatar", class:"btn btn-add2"%>
          <%= f.file_field :image, type:"file", class:"custom-file-input" %>
        </div>
      </div>
      <div class="col-9">
        <div class="field">
          <%= f.password_field :current_password, autocomplete: "current-password", type:"password" %>
          <%= f.label :current_password %> <i>(we need your current password to confirm your changes)</i><br />
        </div>
      </div>
    </div>




  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>





  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>

<h3>Cancel my account</h3>

<p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

      <% if current_user.image.attached?  %>
       <%= image_tag(current_user.image, size: '150') %>
      <% else %>
        <%= image_tag(current_user.image_url, size:'150')%>
      <% end %>
<%= link_to "Back", :back %>

</div>

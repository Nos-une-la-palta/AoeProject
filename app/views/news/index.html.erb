<div class="row mt-2">
  <div class="col-lg-2 offset-lg-10 mb-4">
    <% if can? :create, News %>
      <%= link_to 'Nueva Noticia', new_news_path, class:"btn btn-new-news mb-3"%>
    <% end %>
  </div>
</div>

<div class="row">
  <div class="col-3">
  </div>
  <div class="col-lg-6 col-sm-12 col-md-12">
    <% @news.reverse_each do |news| %>
        <div class="card news-card">
          <img src="https://i.imgur.com/7OO5p0O.png" class="card-img-top" alt="...">
            <div class="card card-news">
              <h4 class="card-title title-h4"><%= news.title %></h4>
              <hr class="hr-news-cards">
              <p class="card-text-1"><%= news.content%></p>
              <hr class="hr-news-cards">
              <p class="card-text"><small class="text-muted float-right"><%= news.created_at.to_date%></small></p>
            </div>
        </div>
        <div class="row">
          <div class="col-lg-4 offset-lg-8 col-sm-4 offset-sm-8 col-md-4 offset-md-8 btn-group">
            <% if can? :create, News %>
             <p><%= link_to 'Editar', edit_news_path(news) , class:"btn btn-edit-news"  %>
              <%= link_to 'Eliminar', news, method: :delete, data: { confirm: '¿Estas seguro?' }, class:"btn btn-destroy-news"  %></p>
           <% end %>
          </div>
        </div>
    <% end %>
  </div>
</div>

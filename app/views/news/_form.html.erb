<%= form_with(model: news, local: true) do |form| %>
  <% if news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news.errors.count, "error") %> prohibited this news from being saved:</h2>

      <ul>
        <% news.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :title, type:"imput2", placeholder:"Titulo" %>
  </div>

  <div class="field">
    <%= form.text_area :content, for:"textarea", id:"textarea", name:"textarea", placeholder:"Contenido" %>
  </div>

  <div class="actions">
    <%= form.submit "Publicar", class:"btn btn-news" %>
  </div>
<% end %>
<div class="links">
  <%= link_to 'Back', news_index_path,  class: "linknew1" %>
</div>
